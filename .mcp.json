{
  "mcpServers": {
    "repo-management": {
      "command": "node",
      "args": ["./plugins/repo-management/dist/index.js"]
    },
    "linear": {
      "command": "npx",
      "args": ["linear-mcp"],
      "env": {
        "LINEAR_ACCESS_TOKEN": "${LINEAR_ACCESS_TOKEN}"
      }
    }
  }
}
